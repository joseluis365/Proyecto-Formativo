[phases.build]
cmds = [
  "apt-get update",
  "apt-get install -y php-bcmath",
  "cd api && composer install --optimize-autoloader --no-dev --no-interaction"
]

[start]
cmd = "cd api && php artisan serve --host=0.0.0.0 --port=$PORT"